
// return keyword is used to actually return
// a result and can put it into a variable.

// whereas console.log simply logs it in the dev console

// function to change first letter in string

function capitalize(are) {
    return str.charAt(0).toUpperCase() + str.slice(1);
}

var city = "paris";
var capital = capitalize(city);

